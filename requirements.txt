# Build-time dependencies for the Jupyter Book companion.
# Used by .github/workflows/deploy-jb.yml and any local rebuild.
#
# Notebook *runtime* dependencies (numpy, scikit-image, cellpose, etc.)
# live in scripts/requirements_notebooks.txt. With execute_notebooks: off
# in _config.yml the build only needs JB + the deploy tool.

# Jupyter Book v2 is a totally different system (MyST-MD-based) and would
# require rewriting _config.yml / _toc.yml. Stay on v1 (Sphinx-based) until
# we choose to migrate deliberately.
jupyter-book>=1.0,<2.0
ghp-import>=2.1
